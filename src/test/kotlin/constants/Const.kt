package constants

/**
 * Multipart part names for POST /user/create (`username`, `email`, `password`).
 */
object Const {
    const val USERNAME = "username"
    const val EMAIL = "email"
    const val PASSWORD = "password"
}
